# Converts 'a' keypress to 255 and 'd' to 0.
# or exits if any other key is pressed.

while true; do
	read -n 1 -s key
	case "$key" in        
		'a') echo '255';;
		'd') echo '0';; 
		*) break
	esac
done
